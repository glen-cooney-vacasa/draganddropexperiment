import React, { Component } from 'react';

class [[NAME]] extends Component {
  constructor(props){
    super(props);

    this.handleClick = this.handleClick.bind(this);

    this.state = {
      //stub
    }
  }

  //NOTE: Use for runtime styles (ie sizing via props)
  getStyle(){
    return {
      //stub
    }
  }

  handleClick(e){
    // stub
    // e.preventDefault();
  }

  componentDidMount(e){
    // stub
    // this.setState({
    //   style: this.getStyle()
    //   })
    // }
  }

  render() {
    return (
      <React.Fragment>
        <div class='event-layer'

          >

        </div>
        {/*stub*/}
      </React.Fragment>
    );
  }
}

export default [[NAME]];
